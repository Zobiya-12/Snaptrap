# Database
DB_HOST=localhost
DB_NAME=snaptrap
DB_USER=snaptrap_user
DB_PASS=changeme

# Flask
FLASK_SECRET=changeme
FLASK_ENV=production

# Grafana
GRAFANA_PASSWORD=changeme

# ngrok (optional)
NGROK_AUTHTOKEN=changeme
